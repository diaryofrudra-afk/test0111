{
  "name": "suprwise-backend",
  "version": "1.0.0",
  "description": "Suprwise FastAPI Python backend - not a Node.js app",
  "scripts": {
    "start": "echo 'This is a Python/FastAPI backend. Run: uvicorn suprwise.main:app --host 0.0.0.0 --port 8000' && exit 1",
    "test": "echo 'Run: pytest' && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
